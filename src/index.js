const diff = require("./diff");
const httpPatch = require("./httpPatch");
const patch = require("./patch");
const utils = require("./utils");

module.exports = {
  diff,
  httpPatch,
  patch,
  utils
};
